name: CI
on:
  push:
    tags-ignore:
      - 'v*'
    branches:
      - '*'
  pull_request:
jobs:
  core:
    name: Native Build (ThroneCore)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Build ThroneCore AAR
        uses: ./.github/actions/throne-core
      - name: Upload ThroneCore AAR
        uses: actions/upload-artifact@v5
        with:
          name: throne-core-aar
          path: app/libs/ThroneCore.aar
          if-no-files-found: error
          retention-days: 1
  build:
    name: Build OSS APK
    runs-on: ubuntu-latest
    needs:
      - core
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Download ThroneCore AAR
        uses: actions/download-artifact@v5
        with:
          name: throne-core-aar
          path: app/libs
      - name: Verify ThroneCore AAR
        run: |
          test -s app/libs/ThroneCore.aar
          unzip -p app/libs/ThroneCore.aar classes.jar > /tmp/throne-core-classes.jar
          unzip -l /tmp/throne-core-classes.jar | grep -q 'io/throneproj/mobile/Mobile.class'
          unzip -l /tmp/throne-core-classes.jar | grep -q 'go/Seq.class'
      - name: Gradle cache
        uses: actions/cache@v5
        with:
          path: ~/.gradle
          key: gradle-oss-${{ hashFiles('**/*.gradle.kts') }}-ci
      - name: Fetch routeprofiles snapshot
        uses: ./.github/actions/routeprofiles
      - name: Gradle Build
        run: |
          echo "sdk.dir=${ANDROID_HOME}" > local.properties
          echo "ndk.dir=${ANDROID_HOME}/ndk/25.0.8775105" >> local.properties
          export LOCAL_PROPERTIES="${{ secrets.LOCAL_PROPERTIES }}"
          ./gradlew app:testOssDebugUnitTest app:assembleOssDebug
          APK=$(find app/build/outputs/apk -name '*arm64-v8a*.apk')
          APK=$(dirname $APK)
          echo "APK=$APK" >> $GITHUB_ENV
      # - name: Upload Artifacts
      #   uses: actions/upload-artifact@v5
      #   with:
      #     name: APKs
      #     path: ${{ env.APK }}
