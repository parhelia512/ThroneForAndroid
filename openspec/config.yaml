schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
context: |
  ThroneForAndroid (nb4a) 是基于 SagerNet 代码线的 Android 代理客户端（v2.0.0 起，包名 com.nb4a.throne）。
  Android 端使用 Kotlin/Java 与传统 View/Fragment；代理核心为 throneproj/Throne 桌面同源核心，
  以 nb4a.properties 的 THRONE_CORE_REF 为唯一来源，由 CI 构建 ThroneCore.aar 与 sing-box 选项 schema，
  应用版本以 nb4a.properties 的 VERSION_NAME/VERSION_CODE/PACKAGE_NAME 为唯一真实来源。
  本地不安装 Go、不克隆核心仓库，也不执行 Android/Go 编译；构建与真机验证交给 GitHub Actions。
  项目采用快速滚动验证：一次只实现一个最小自洽批次（可由同一节内多个实现、测试和提交子任务共同组成），
  完整实现该批次并随即交给 CI/真机验证；收到结果前不得继续累积下一批次的代码修改。
  外部依赖/API 调研应联网查阅官方源码和文档，不假设存在本地依赖仓库；需要参考外部仓库本地副本时，
  必须先向用户说明目的并由用户提供明确路径，不得自行遍历用户目录。
  仓库当前能力规范位于 openspec/specs/；历史大型迁移记录位于 openspec/history/。
  仓库维护用 Python 工具只允许放在 tools/diagnostics/，不得放入 .roo/、openspec/ 或仓库根目录；
  Python 脚本必须先写成文件，再从仓库根目录使用 uv run <脚本路径> 执行，禁止 python/python3/python -c。

rules:
  proposal:
    - 明确指出受影响的 openspec/specs 能力规范以及是否涉及 throne 核心、Android、构建链或开发工具布局。
    - 涉及外部 API 时记录官方来源与目标版本，不依赖未纳入仓库的本地源码。
  specs:
    - 使用 RFC 2119 风格的 MUST/SHALL 表达可验证要求。
    - 每条 requirement 至少包含一个 WHEN/THEN 场景（可按需补充 GIVEN 前置条件）。
  tasks:
    - 将实现拆成可独立审查、回退和验证的最小自洽批次；一个任务不得混合多个可独立验证的行为变化。
    - 用二级编号标题表示批次边界；标题下的 1.1、1.2 等复选项表示该批次的实现、测试、提交和验证步骤，不表示每个复选项完成后都要暂停。
    - 每个实现批次后立即安排对应的 CI/真机验证阶段，再列下一实现批次；不得把所有验证集中到变更末尾。
    - 验证阶段应写明适用的 CI workflow/job、真机场景、预期结果和需要回传的证据；不适用的验证必须注明原因。
    - 将规范同步和本地静态校验纳入对应实现批次，不要为形式完整而制造不提供反馈价值的独立任务。

operations:
  apply:
    guidance:
      - 每次调用处理一个完整的最小自洽批次：连续完成同一编号章节内为形成可审查改动所需的实现、回归测试和提交/触发验证步骤，然后等待该批次的 CI/真机验证结果；不得在 1.1、1.2 等子步骤之间机械暂停，也不得继续实现下一编号批次。
      - 开始新批次前先处理上一批次的 CI/真机结果；失败时只修复当前批次，不得推进后续任务。
      - 本地仅运行不依赖 Go/Android SDK 的静态校验；不得用本地推断替代 CI/真机证据。
      - 没有实际验证结果时，不得勾选 CI/真机验证任务；应报告待执行验证、预期结果和需要回传的最小证据。
      - 若改变架构、关键模块、协议、配置、构建流程或维护工具布局，同步更新对应主规范。
      - Python 辅助程序放入 tools/diagnostics/，通过 uv run 执行；不得向 .roo/ 或 openspec/ 写入运行脚本。
  archive:
    guidance:
      - 归档前确认 delta spec 已准确合入主规范，并区分自动校验结果与待 CI/真机验证事项。
